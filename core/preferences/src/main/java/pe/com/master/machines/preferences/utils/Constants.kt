package pe.com.master.machines.preferences.utils

import pe.com.master.machines.preferences.BuildConfig

object Constants {

    const val NAME_PREFERENCES = "Pichincha"

    object Preferences {
        val PREF_VISITOR = "${BuildConfig.LIBRARY_PACKAGE_NAME}.pref_visitor"
    }
}